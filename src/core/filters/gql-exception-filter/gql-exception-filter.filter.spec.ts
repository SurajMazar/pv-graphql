import { GqlExceptionFilter } from './gql-exception.filter';

describe('GqlExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new GqlExceptionFilter()).toBeDefined();
  });
});
